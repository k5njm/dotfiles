- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.gitconfig: 
    ~/.oh-my-zsh:
    ~/.tmux.conf: 
    ~/.vimrc:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
